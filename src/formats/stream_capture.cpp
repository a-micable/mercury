#include "framed_format.hpp"

namespace mercury::formats {
// Stream capture files are parsed by the shared frame reader; live streaming decoders can
// reuse this unit when incremental buffering is added.
}
