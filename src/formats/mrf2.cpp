#include "framed_format.hpp"

namespace mercury::formats {
// MRF2 is registered through FramedParser with a wider version window. Format-specific
// extensions can layer additional record decoders here without changing registry callers.
}
